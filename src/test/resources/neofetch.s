alicia:                                 # @alicia
        addi    a0, zero, 34
        sb      a0, 1032(zero)
        addi    a1, zero, 72
        sb      a1, 1028(zero)
        addi    a0, zero, 101
        sb      a0, 1028(zero)
        addi    a2, zero, 108
        sb      a2, 1028(zero)
        sb      a2, 1028(zero)
        addi    a3, zero, 111
        sb      a3, 1028(zero)
        addi    a4, zero, 32
        sb      a4, 1028(zero)
        addi    a5, zero, 119
        sb      a5, 1028(zero)
        sb      a3, 1028(zero)
        addi    a5, zero, 114
        sb      a5, 1028(zero)
        sb      a2, 1028(zero)
        addi    a6, zero, 100
        sb      a6, 1028(zero)
        addi    a7, zero, 33
        sb      a7, 1028(zero)
        addi    t0, zero, 10
        sb      t0, 1028(zero)
        sb      a1, 1028(zero)
        sb      a0, 1028(zero)
        sb      a5, 1028(zero)
        sb      a0, 1028(zero)
        sb      a4, 1028(zero)
        addi    t1, zero, 99
        sb      t1, 1028(zero)
        sb      a3, 1028(zero)
        addi    a5, zero, 109
        sb      a5, 1028(zero)
        sb      a0, 1028(zero)
        addi    a1, zero, 115
        sb      a1, 1028(zero)
        sb      a4, 1028(zero)
        addi    t2, zero, 116
        sb      t2, 1028(zero)
        addi    a1, zero, 117
        sb      a1, 1028(zero)
        addi    a1, zero, 120
        sb      a1, 1028(zero)
        sb      a7, 1028(zero)
        sb      t0, 1028(zero)
        addi    a1, zero, 97
        sb      a1, 1028(zero)
        sb      a2, 1028(zero)
        addi    a2, zero, 105
        sb      a2, 1028(zero)
        sb      t1, 1028(zero)
        sb      a2, 1028(zero)
        sb      a1, 1028(zero)
        addi    a1, zero, 64
        sb      a1, 1028(zero)
        sb      a6, 1028(zero)
        sb      a0, 1028(zero)
        sb      a5, 1028(zero)
        sb      a3, 1028(zero)
        sb      a4, 1028(zero)
        addi    a1, zero, 126
        sb      a1, 1028(zero)
        addi    a1, zero, 62
        sb      a1, 1028(zero)
        sb      a4, 1028(zero)
        addi    a1, zero, 110
        sb      a1, 1028(zero)
        sb      a0, 1028(zero)
        sb      a3, 1028(zero)
        addi    a1, zero, 102
        sb      a1, 1028(zero)
        sb      a0, 1028(zero)
        sb      t2, 1028(zero)
        sb      t1, 1028(zero)
        addi    a0, zero, 104
        sb      a0, 1028(zero)
        sb      t0, 1028(zero)
        ret